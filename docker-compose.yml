services:
  figextract:
    image: nginx:stable
    container_name: figextract
    restart: unless-stopped
    ports:
      - "8088:80"
    volumes:
      - ./figma-image-extractor.html:/usr/share/nginx/html/index.html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
